# Project Guide

소스는 `src/agent_learning` 아래에 둡니다.

- `fake.py`: 외부 API 없는 fake chat/streaming model
- `llm/`: chapter별 service
- `tools/`: calculator 같은 local tool
- `examples/`: chapter 실행 entrypoint
- `testdata/docs/ch09-rag`: RAG 예시 문서
